import { PlaceholderPage } from '@/components/PlaceholderPage'

/**
 * Feature: booking form for a service. Lazy-loaded as its own chunk.
 */
export default function BookingPage() {
  return <PlaceholderPage title="Booking" />
}
