import { PlaceholderPage } from '@/components/PlaceholderPage'

/**
 * Feature: confirmation screen after a successful booking. Lazy-loaded as its
 * own chunk.
 */
export default function ConfirmationPage() {
  return <PlaceholderPage title="Confirmation" />
}
