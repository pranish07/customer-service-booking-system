import { PlaceholderPage } from '@/components/PlaceholderPage'

/**
 * Feature: browse/list services. Lazy-loaded as its own chunk.
 */
export default function ServiceListPage() {
  return <PlaceholderPage title="Service List" />
}
