import { PlaceholderPage } from '@/components/PlaceholderPage'

/**
 * Feature: list of the customer's existing bookings. Lazy-loaded as its own
 * chunk.
 */
export default function MyBookingsPage() {
  return <PlaceholderPage title="My Bookings" />
}
