import { PlaceholderPage } from '@/components/PlaceholderPage'

/**
 * Feature: view a single service's details. Lazy-loaded as its own chunk.
 */
export default function ServiceDetailsPage() {
  return <PlaceholderPage title="Service Details" />
}
